# CircuitLab (emb3d) — Bug Report & Fix Plan

> **Archived (2026-09-18).** Every issue here is fixed. It is kept for its history and its issue codes, which the code and tests still name. New bugs go in [BUGS.md](../tracking/BUGS.md); new work is planned in [ROADMAP.md](../planning/ROADMAP.md).

_Scanned: 2026-09-12 · Files reviewed: all of `index.html`, `src/`, `js/`, `css/`, `dist/`, `package.json` · Re-read in full for #31 (2026-09-17), which added D31–D48, E20–E22, F11 and F12._

## How this was checked

1. **Static check** — every `getElementById('...')` in the JS was compared with the `id="..."` values in `index.html`.
2. **Live check** — the app was run with `npm run dev`, opened in headless Chrome, and a script clicked every menu item, button, search box and keyboard shortcut while recording every error.
3. **Dependency check** — `npm audit`.

## Summary

| Area | Status |
|---|---|
| App loads, background animates | ✅ Works; one animation draws it, not two (#21); it follows the theme and a window resize (#22c) |
| Dashboard (home screen) | ✅ Stats, quick access, recently viewed, sample projects (#8) |
| Left menu switches screens | ✅ Works; the address follows the screen, so refresh, Back/Forward and links work (#22) |
| 3D viewer | ✅ New layout (#9): part info, controls, full pin table, pin details, tooltip; canvas fills its area. Every part has its own model shape with its real pin count, its name printed on the chip, and old models are freed instead of leaking (#23). A resistor, capacitor and LED have their own models with clickable leads (#23b) |
| Circuit simulator | ✅ Layout, toolbar, palette (with a battery) and status bar work (#14). Circuits give correct results (#14b): an LED lights only in a closed loop and the right way round, and currents follow Ohm's law. The oscilloscope and multimeter show real circuit values, and the scope's ON button and dials work (#14c). It is set up once and draws only while visible, with a real-time clock (#15) |
| Component Database | ✅ Component Library (#10): category filter, sort, compare, part cards with "View 3D". 13 parts, including a resistor, capacitor and LED checked against their datasheets (#23b) |
| Board Explorer | ✅ Styled layout (#16); pin clicks, highlighting and resizing work (#16b); all 8 tabs show their own board, with pinouts from official sources (#16c) |
| Datasheet Viewer | ✅ Shows datasheets. Sidebar, section bar and layout styled (#17); the screen no longer cuts off its bottom. The search box filters the list and all 7 section buttons show real content, including a Pinout table and package sizes (#17c) |
| AI Assistant | ✅ Chat works (#11), picks the right stored answer (#12), shows typed text safely and code blocks correctly (#13). All 3 suggestion chips have real answers, checked against the datasheets (#12b), and replies show lists and tables properly (#13b) |
| Projects | ✅ Shows the 6 example projects, and you can make, open and delete your own; they are kept in the browser (#18) |
| Search (Ctrl+K) | ✅ Works, and ↑↓ / ↵ move through and open results (#20) |
| Top bar buttons | ✅ New Project opens the new-project form, and Share copies a link to the current screen (#22d) |
| Settings (dark mode) | ✅ Both theme buttons switch between light and dark, and the choice is remembered (#19) |
| Keyboard shortcuts | ✅ Every listed shortcut works (1–9, / Ctrl/⌘ K, ?, W, E, R, Space, Esc) |

_Status after #32: **every issue in this report is fixed.** Every screen works with no console errors, in both themes and on a high-DPI screen; 220 smoke tests, 0 lint warnings, 0 `npm audit` issues, and a build with no warnings. `data.js`, `main.css` and `app.js` are split into ES modules (#27a–c), Three.js is downloaded only for the 3D Viewer (E18), and what is left in the **Future ideas** list below is new work, not bugs._

**Root cause:** `app.js` and the CSS were written together for a page layout that isn't in this project, and most of `index.html` was built separately (F1). On top of that, `data.js` was never loaded (A1, fixed in #3). **Which markup each screen uses is decided in [ADR 0002](../decisions/0002-screen-markup.md).**

> **Paths:** this report was written before the restructure. File paths below use the new layout (see [ARCHITECTURE.md](../planning/ARCHITECTURE.md)). Links point at the file, not a line: the code has moved a lot since, so old line numbers would land on the wrong code (E22, and again after #31). _Since #27a, `data.js` is split into `src/data/*.js`, so older `data.js:NN` links point to a file that no longer exists; search the part, board or datasheet name in `src/data/` instead. Since #27c the same goes for `app.js:NN` links (the code is in `src/app`, `ui`, `views`, `services` and `utils`) and for the 3D builders (`src/engines/three-viewer/models/`)._

---

## Part 1 — Everything that is broken

Severity: 🔴 Critical (crash / feature dead) · 🟠 High (feature wrong) · 🟡 Medium · ⚪ Low (cleanup)

### A. Crashes (seen live in the browser)

| # | Sev | Problem | Where | Error seen |
|---|---|---|---|---|
| A1 | ✅ | `data.js` is never loaded, so `CircuitLabData` does not exist. This one bug causes A2–A8. _Fixed in #3: imported in `src/main.js`._ | [src/main.js](../../src/main.js) | `ReferenceError: CircuitLabData is not defined` |
| A2 | ✅ | 3D Viewer: no part is selected, so it crashes reading `.id` of nothing. _Fixed by A1: the first part is selected at startup. No extra guard was added, because it would only hide a future data-loading failure, and the "component data is loaded" test catches that._ | [app.js:644](../../src/app/app.js) | `TypeError: Cannot read properties of null (reading 'id')` |
| A3 | ✅ | 3D model loader also needs `CircuitLabData`. _Fixed by A1._ | [three-viewer/index.js:726](../../src/engines/three-viewer/index.js) | (silently never called because of A2) |
| A4 | ✅ | Board Explorer crashes. _Fixed by A1._ | [app.js:983](../../src/app/app.js) | `ReferenceError` |
| A5 | ✅ | Datasheet Viewer crashes. _Fixed by A1._ | [app.js:1252](../../src/app/app.js) | `ReferenceError` |
| A6 | ✅ | Projects crashes. _Fixed by A1._ | [app.js:1498](../../src/app/app.js) | `ReferenceError` |
| A7 | ✅ | Search (Ctrl+K or `/`) crashes on typing. _Fixed by A1._ | [app.js:362](../../src/app/app.js) | `ReferenceError` |
| A8 | ✅ | AI Assistant crashes when you send a message. _Answer engine fixed by A1. The Send button is still broken (B1, B3), and answers are wrong (D1)._ | [app.js:1429](../../src/app/app.js) | `ReferenceError` |

### B. Code looks for page elements that don't exist (name mismatch)

`app.js` asks for these IDs, but `index.html` uses different names. Result: that part of the screen stays empty.

> Per [ADR 0002](../decisions/0002-screen-markup.md): **B4–B7** are fixed by switching the Viewer to the new layout (the page gets the IDs the JS expects). **B8** is fixed by adding the Dashboard and showing the Component Library in the Database screen. **B1–B3 and B9** are fixed on the JS side (those screens keep the current page).

| # | Sev | Code looks for | Page actually has | Where in code |
|---|---|---|---|---|
| B1 | ✅ | `ai-chat-area` | `ai-chat-messages` _**Fixed in #11:** messages show in the chat (tested: Send, Enter, chips, Datasheet "Ask AI"), below the welcome message, which now stays._ | [app.js:1470](../../src/app/app.js) |
| B2 | ✅ | `ai-input` | `ai-user-query` _**Fixed in #11:** Enter sends exactly one message and clears the input (tested)._ | [app.js:1442](../../src/app/app.js) |
| B3 | ✅ | `ai-send` | `ai-send-btn`. The only working hook is a page-wide click listener that checks `e.target.id === 'ai-send-btn'`, so clicking the **arrow icon** inside the button (where most people click) sends nothing. _Found by the smoke tests._ _**Fixed in #11:** the button is wired by its ID, so the icon works too (tested). The page-wide click and `keypress` listeners are removed; they only avoided double sends by accident (the first handler empties the input)._ | [app.js:1441](../../src/app/app.js) |
| B4 | ✅ | `viewer-sidebar` | nothing — closest is `component-list` / `pin-info-panel` _**Fixed in #9:** the Viewer now uses the new layout (ADR 0002)._ | [app.js:649](../../src/app/app.js) |
| B5 | ✅ | `pin-table-body` | nothing — closest is `pin-details` _**Fixed in #9:** the Viewer now uses the new layout (ADR 0002)._ | [app.js:722](../../src/app/app.js) |
| B6 | ✅ | `pin-detail-panel` | `pin-info-panel` _**Fixed in #9:** the Viewer now uses the new layout (ADR 0002)._ | [app.js:756](../../src/app/app.js) |
| B7 | ✅ | `pin-tooltip` | nothing _**Fixed in #9:** the tooltip element exists and shows on pin hover (tested)._ | [app.js:1598](../../src/app/app.js) |
| B8 | ✅ | `view-dashboard`, `view-components` | these screens don't exist in the page _**Dashboard part fixed in #8:** it is added as the home screen. The Component Library part follows in #10 (`fix/database-library`)._ _**Fully fixed in #10:** the library now draws into the Database screen, and the dead `components` view is removed._ | [app.js:386](../../src/app/app.js), [app.js:502](../../src/app/app.js) |
| B9 | ✅ | `mm-value` / `mm-unit` / `mm-mode-select` | `multimeter-val` / `multimeter-unit` / `mm-mode` _**Fixed in #14:** the engine uses the page's IDs, so the multimeter updates (tested: a resistor reads 1.00 kΩ)._ | [simulator.js:1140](../../src/engines/simulator/index.js) |

### C. Buttons on the page with no code behind them

These are visible and clickable, but **nothing happens** (confirmed by clicking them in the live test).

| # | Sev | Screen | Dead buttons / panels |
|---|---|---|---|
| C1 | ✅ | 3D Viewer | Part list (`component-list`), filter box (`component-filter`), category buttons, Rotate, Wireframe, Explode, Pins, Reset camera, Screenshot, package switcher, HUD values, pin panel close. _ADR 0002: replaced by the new layout's working controls (part switcher, Solid/Wire/Explode, auto-rotate, zoom/reset). The package buttons, HUD and screenshot button are dropped (see Future ideas)._ _**Fixed in #9:** part switcher, Solid/Wire/Explode, auto-rotate and zoom/reset all work (tested)._ |
| C2 | ✅ | Simulator | Run, Pause, Stop, Clear, Export, Speed slider, Upload code, all "Add Resistor / LED / Capacitor / IC / Wire" buttons. The simulator's `startSim()` is **never called** by anything, so a circuit can never run. _Found in #14: the palette had no battery (no power source at all), "Wire" and "NE555 IC" aren't simulator parts, drag-from-palette read the wrong attribute, Stop was always disabled, and the status bar never changed._ _**Fixed in #14:** every control is wired once (tested). A Battery button is added to the palette. Wire and NE555 IC explain themselves; Upload Code says it isn't available yet. Pause keeps the time, Stop resets it, and the buttons are enabled only when they can be used. The status bar shows Running / Paused / Ready and the time._ |
| C3 | ✅ | Database | Component grid (`db-components-grid`), Compare button, comparison table. _ADR 0002: replaced by the Component Library (grid, filters, compare, sort)._ _**Fixed in #10:** the Database screen shows the Component Library (tested: all cards, filter, compare, View 3D)._ |
| C4 | ✅ | Projects | "Create project" button (`create-project-btn`), project grid (`projects-grid`) _Re-checked in #18: the grid and "Open →" already worked (fixed by A1 in #3 and the render in #8); the only dead part left was **New Project**, which showed "Project creation coming soon!"._ _**Fixed in #18:** the button opens a "name your project" form as the first card (Enter creates, Esc cancels). A named project is added to the grid, kept in the browser under `circuitlab.my-projects` so it survives a reload, opens the Simulator, and can be deleted with the ✕ on its card. A blank name is refused and the form stays open. The name is escaped everywhere it is shown, so typed HTML is text (tested). Styles are in the new `src/styles/views/projects.css`._ |
| C5 | ✅ | Settings | "Toggle Dark Mode" (`theme-btn-toggle`), top-bar theme toggle. `state.theme` existed but nothing ever read it. _Measured in #19: a real light theme needed more than a class — `main.css` had **60 hard-coded colour values outside `:root`**, 12 of them dark surfaces (`rgba(10,10,20,0.95)` ×9, the glass panel, the overlay, `background:#000` ×3) that would have stayed dark._ _**Fixed in #19:** those 12 became tokens (`--bg-panel`, `--bg-panel-soft`, `--bg-overlay`, `--glass`, `--bg-code`), and a `:root[data-theme="light"]` block overrides the palette. Both buttons toggle, the button says what it will do, and the choice is kept under `circuitlab.theme`. The 3D viewport, the simulated multimeter display and code blocks stay dark on purpose. **Also found by looking at a screenshot:** the 16 `<kbd>` keys in Settings and the shortcuts pop-up carried an inline `background:#222`, so in light mode they were black boxes with invisible text; they are now styled with tokens in `panels.css` and a test checks the text stands out in both themes._ |
| C6 | ✅ | Top bar | Notifications drawer, "Clear all", shortcuts modal (nothing can open it) _Re-checked in #20: the shortcuts modal already opens with `?` (fixed in #4), and the unread dot on the bell had no CSS at all — 0 px wide and transparent. The bell only showed a "No new notifications" toast; the drawer markup existed but nothing opened it._ _**Fixed in #20:** the bell opens and closes the drawer, Esc and a click anywhere else close it, and "Clear All" empties the list, shows "Nothing new right now." and hides the unread dot. The three items moved from hard-coded markup into `app.js` so they can be cleared. The drawer's inline styles (with `#222` and `#00d4ff`) moved to the new `src/styles/components/notifications.css` with design tokens — it would otherwise have stayed dark in the light theme added in #19._ |
| C7 | ✅ | Simulator | The oscilloscope ON button and the V/div and T/div dials do nothing, and "Nodes: 0" in the status bar never changes. _Found in #14._ _**Nodes part fixed in #14b:** the status bar shows the real number of nets (tested). The oscilloscope part moves to #14c with D21._ _**Fixed in #14c:** the ON button turns the screen off and on, and the V/div (0.5–10 V) and T/div (10 ms–1 s) dials step through their values, update their readouts and change the traces (tested)._ |
| C8 | ✅ | Datasheet | The **search box** (`ds-search`) does nothing. No JS anywhere references it: `grep -n "ds-search" src/app/app.js` returns no matches, so typing in it never filters the list. _Found in #17 while styling the sidebar._ _**Fixed in #17c:** a new `renderDatasheetList()` draws the list and keeps only the datasheets whose name or maker contains the typed text, ignoring case; an empty result shows "No datasheet matches …" (the query is escaped). The `input` handler is added on the first visit only, with the `_wired` guard, so repeat visits don't pile up handlers (tested: filtering still matches one datasheet after 3 visits)._ |
| C9 | ✅ | Top bar | The **New Project** (`new-project-btn`) and **Share** (`export-btn`) buttons have no code: nothing in `src/` mentions either ID, so clicking does nothing. _Found in #22 while reading `app.js` (search only, not clicked)._ _Confirmed in #22d by clicking both: the screen, the address and the toasts stayed exactly the same._ _**Fixed in #22d:** New Project opens the Projects screen with the "name your project" form ready to type in (the existing `newProject()`). Share copies the page link — which since D9 opens the same screen — and says "Link copied"; if the browser blocks the clipboard, the toast shows the link instead (escaped). Both wired once at startup (tested)._ | [app.js:1896](../../src/app/app.js), [index.html:185-195](../../index.html) |

### D. Logic bugs (code runs but does the wrong thing)

| # | Sev | Problem | Where |
|---|---|---|---|
| D1 | ✅ | **AI always gives the wrong answer.** It matches only the first or second word of each stored question. Any message with "what" gets the LED-resistor answer; anything with "how" or "do" gets the I2C answer. Example: _"how does an esp32 work"_ → I2C wiring guide. _Measured in #12: 19 of 34 test questions right. Even the stored question "Explain how an ESP32 works" got the I2C guide._ _**Fixed in #12:** a named part gets its card first (punctuation ignored, so "MPU-6050" = "mpu6050"); otherwise the stored answer with the most whole-word keyword hits wins, specific topics first on a tie; no hits gives the "be more specific" reply. 59 of 60 test questions right (the miss is a nickname, see Future ideas). Tested in `ai.spec.js`._ | [app.js:1519-1561](../../src/app/app.js) |
| D2 | ✅ | **Keyboard shortcuts are wrong.** Keys `1` and `2` go to "dashboard" and "components", which don't exist, so you get a blank screen. The shortcuts shown to the user (`W`, `E`, `R`, `Space`) are not coded at all. _Also found: ⌘K didn't work, Ctrl+3 (a browser shortcut) switched screens, `?` couldn't open the list, and Space re-clicked the focused sidebar button._ **Fixed in #4:** `1–8` follow the sidebar order; `W`/`E`/`R` work on the Viewer; `Space` works on the Simulator; `?` opens the list; Ctrl/⌘+K and `/` open search; keys are ignored while typing, with Ctrl/⌘/Alt, and on auto-repeat. Both shortcut lists now show every key. | [app.js:1526](../../src/app/app.js) |
| D3 | ✅ | **Simulator gets set up again on every visit.** Each time you open the Simulator screen, it adds another set of mouse handlers and another endless drawing loop. After 5 visits one click adds 5 parts, and the CPU use keeps growing. _**Corrected in #15 (measured):** parts are **not** added twice. The canvas handlers are named functions, and the browser ignores adding the same function again (one drag after 3 visits adds 1 part; #14's note saying otherwise was wrong). What did pile up is the drawing loop: 3 visits → 3 loops, 99 frames/s instead of 33, and a clock running 3× too fast._ _Note from #14c (read in the code, not yet tested): `resizeCanvas` sizes the board from its box, and a hidden board's box is 0×0. Today each visit sets the engine up again, which hides this. Once #15 sets it up only once, a window resize while the Simulator is hidden must not shrink the board to 0×0._ _**Fixed in #15:** `init()` runs on the first visit only (and refuses to run twice), without the old 100 ms wait. The engine resizes its canvases on the first frame the board is visible again, which covers the first visit and a window resize while hidden (tested: one drawing loop after 3 visits; the board keeps its real size)._ | [app.js:878](../../src/app/app.js), [simulator.js:619](../../src/engines/simulator/index.js) |
| D4 | ✅ | **Two background animations draw on the same canvas** (`circuit-bg`), which uses double the CPU and can flicker. _Measured in #21: the canvas was cleared **108 times in 54 frames** — two full redraws every frame. It did not flicker: `circuit-bg.js` drew first, then `initBackground()` in `app.js` cleared the canvas and drew over it, so users only ever saw the `app.js` dots and the other animation was pure wasted work._ _**Fixed in #21:** `initBackground()` is removed from `app.js` and `circuit-bg.js` (the PCB grid with moving electrons, in its own engine file) is the only background. 1 clear per frame now (tested). One lint warning fewer (cap 13 → 12). **This changes what the background looks like**, from floating dots to the circuit grid. Two bugs in the kept file were found here and logged as D28 and D29._ | [circuit-bg.js](../../src/engines/background/circuit-bg.js), `tests/smoke/background.spec.js` |
| D5 | ✅ | 3D and simulator drawing loops keep running when their screen is hidden, which wastes battery. _ESLint confirms: `animationId` and `animId` are stored but never used to cancel the loops._ _**3D part fixed in #9:** the 3D scene is not drawn while its screen is hidden (0 frames vs 38/s before, tested). The simulator loop is still open (#15)._ _**Simulator part fixed in #15:** the board is not drawn while its screen is hidden, using the same check as the 3D Viewer (60 frames/s before, 0 after, tested). The simulation pauses while hidden. The unused `animId` is removed._ | [three-viewer.js:957](../../src/engines/three-viewer/index.js), [simulator.js:938](../../src/engines/simulator/index.js) |
| D6 | ✅ | **Unsafe HTML in AI chat.** The user's typed text is put into the page as raw HTML (`escapeHtml` exists but isn't used here). Typing `<img src=x onerror=alert(1)>` would run code. Low risk today (no login or server), but a bad habit to fix now. _Since #11 the chat shows messages, so this really happens (confirmed in a scratch copy)._ _**Fixed in #13:** `formatMarkdown` escapes the text first, so typed HTML shows as text and never runs (tested). The chat was the only place typed text reached `innerHTML` (search never shows the query). `escapeHtml` now also escapes `"` and `'`, so it is safe inside attributes too._ | [app.js:1479](../../src/app/app.js), [app.js:1754](../../src/app/app.js), [app.js:1768](../../src/app/app.js) |
| D7 | ✅ | Markdown formatter runs the `` `inline` `` rule before the ```` ```block``` ```` rule, so code blocks in AI answers come out broken. _Visible since #11: they show as small empty boxes._ _Measured in #13: all 7 code blocks in 4 answers became 14 empty boxes, and `#include <Wire.h>` lost `<Wire.h>`._ _**Fixed in #13:** code blocks are set aside before the other rules and put back last, so all 7 show with their exact code (tested). A CSS rule stops the inline-code box from appearing on every line inside a block._ | [app.js:1754](../../src/app/app.js), [main.css:1985](../../src/styles/) |
| D8 | ✅ | The 3D loader has cases for `nrf52840` and `bme280`, which aren't in the data. Parts that are in the data (`l298n`, `ams1117`, `nrf24l01`) all fall back to a plain 8-pin chip. _ESLint found more: 4 finished models (`buildArduinoUno`, `buildResistor`, `buildCapacitor`, `buildLED`) are never called, so they can never be shown._ | [three-viewer.js:739-770](../../src/engines/three-viewer/index.js), [three-viewer.js:423](../../src/engines/three-viewer/index.js), [:553](../../src/engines/three-viewer/index.js), [:590](../../src/engines/three-viewer/index.js), [:628](../../src/engines/three-viewer/index.js) _**Fixed in #23:** the `nrf52840` and `bme280` cases are removed (neither is in the data), and the three parts that fell back to a generic 8-pin chip get their own shape: **L298N** a Multiwatt-15 and **AMS1117** a SOT-223 through a new `buildSIP()` single-row builder with a metal tab, **nRF24L01+** a DIP-8-style module. The fallback now follows the part's real pin count (even → dual row, odd → single row) instead of always drawing 8 pins. **Also found here:** `buildESP32()` and `buildHCSR04()` built pin headers but never tagged them and then cleared `pinMeshes` straight after, so the **ESP32 (38 pins) and HC-SR04 (4 pins) had no interactive pins at all** — no hover tooltip, no click highlight, and the pin table could not highlight anything. Both fixed. A test now checks every part's model draws exactly as many pins as the part really has._ _**Follow-up in #23b:** `buildResistor`, `buildCapacitor` and `buildLED` are used now, for three new parts (CFR-25 resistor, ECA-1EM101 capacitor, WP7113ID LED). Like the ESP32 here, their leads were never tagged as pins, so they are now pins 1 and 2 (hover and highlight work). The LED model was green; it is red, like the WP7113ID. `buildArduinoUno` is still unused: the Uno is a board, not a component._ |
| D9 | ✅ | The HTML has `onclick="window.location.hash='#simulator'"`, but the app has no URL/hash routing, so it does nothing. _Measured in #22: a sidebar click left the address with no `#`; setting `#simulator` by hand did nothing; opening `/#ai` fresh showed the Dashboard; a refresh on the Simulator went back to the Dashboard; Back changed the address but not the screen. The "Open Workspace" button with that `onclick` is never even visible: it sits in `#projects-grid`, which `renderProjects()` replaces when the Projects screen opens (1 in the page before, 0 after)._ _**Fixed in #22:** `navigateTo()` keeps `#<screen>` in the address, and a `hashchange` listener switches the screen, so refresh, Back/Forward, links and typed addresses all work, and every way of switching (sidebar, keys 1–9, cards, search, "View 3D") goes through it. Each switch is a Back step. An unknown screen (`#nope`) used to be able to hide every screen; it now falls back to the Dashboard and **replaces** the bad entry, so Back never lands on it again. Tested in `routing.spec.js` (7 tests, all 7 failed before)._ | [app.js:124](../../src/app/app.js), [index.html:544](../../index.html) |
| D10 | ✅ | `sortComponents(by)` ignores `by`, so the sort dropdown does nothing. _Found by ESLint._ _**Fixed in #10:** sorts by name, pin count or lowest voltage, and the dropdown keeps the choice (tested)._ | [app.js:1704](../../src/app/app.js) |
| D11 | ✅ | **Chip names are never drawn on the 3D chips.** `buildDIP` and `buildQFP` take a `label` (e.g. "ATmega328P") but never use it. _Found by ESLint._ | [three-viewer.js:295](../../src/engines/three-viewer/index.js), [three-viewer.js:368](../../src/engines/three-viewer/index.js) _**Fixed in #23:** `makeLabelMesh()` draws the name onto a canvas and uses it as a texture on the top of the chip, for both `buildDIP` and `buildQFP` (and the new `buildSIP`). Long names are shrunk to fit. Checked on a screenshot: "ATmega328P" is readable on the chip._ |
| D12 | ✅ | Simulator leftovers: `mmEnabled` is never read, so the multimeter on/off flag does nothing. `posNode` / `negNode` are worked out in `runSimulation` but never used. Check whether battery polarity is ignored. _Found by ESLint._ _**Confirmed in #14** (engine test): an LED lights with only the battery's + wired to it (no loop back to −), and also when it is reversed in a closed loop. Moved to its own branch (#14b), because it needs a new voltage solver._ _Measured in #14b with 6 test circuits: the LED lit in all 6, and a 1 kΩ resistor gave 40.7 mA (should be 6.93 mA), because the old solver only followed wires out of the + terminal, faked resistors as a "10% drop" and assumed 150 Ω for every LED._ _**Fixed in #14b:** a DC solver (nets + nodal analysis). LEDs and diodes conduct only forwards, capacitors block DC, and all 6 circuits match hand calculations (tested). The multimeter's current is the battery's current, and wires animate only when current flows. `mmEnabled` (never read) is removed. 6 lint warnings fewer (cap 24 → 18)._ | [simulator.js:818-931](../../src/engines/simulator/index.js) |
| D13 | ✅ | **Explode destroyed the 3D model.** The target position was calculated from the saved position **before** it was saved (`undefined + 0.3 = NaN`), so the parts vanished and never came back. Toggling quickly also made two animations fight (parts bounced). _Found in #4._ **Fixed in #4:** save first (checking for `undefined`, since 0 is valid), and stop the previous animation before starting a new one. _Follow-up in #9: the explode and grow-in animations are now time-based, so they last ~320 ms / ~200 ms even when frames are slow._ | [three-viewer.js:920](../../src/engines/three-viewer/index.js) |
| D14 | ✅ | The search popup footer shows **↑↓ Navigate** and **↵ Select**, but those keys do nothing (only Esc is handled). _Found in #4._ _**Fixed in #20:** ↑ and ↓ move a highlight through the results and wrap around at both ends, ↵ opens the highlighted one, and a new search starts at the top again. The highlight is a `.selected` class styled with the design tokens, and the row is scrolled into view._ | [app.js:314](../../src/app/app.js) |
| D15 | ✅ | **The 3D Viewer is empty when the app first opens.** At startup the app asks for the model before the 3D engine is ready (the engine starts 300 ms later), and nothing asks again. The chip only appears after leaving and returning to the Viewer. The "Rendering 3D Model..." text also never hid. _Found in #4 (`getModelBounds()` returns `null` at first load)._ **Fixed in #4b:** a `showSelectedModel()` helper draws the part and hides the loading text. It's called when the Viewer opens **and** as soon as the engine is ready. | [app.js:651](../../src/app/app.js), [app.js:79](../../src/app/app.js) |
| D16 | ✅ | **Each visit to the Viewer leaks graphics memory.** Every visit rebuilds the model, and the old one is removed with `scene.remove()` but never freed with `dispose()`. Measured: **2 → 297 geometries after 5 visits** (about 59 per visit). _Found in #4b._ | [three-viewer.js:733](../../src/engines/three-viewer/index.js) _**Fixed in #23:** a new `disposeObject()` frees every geometry, material and texture (including a highlighted pin's saved material) when a model leaves the scene. Measured with a new `getMemoryInfo()` hook: **61 → 61 geometries after 5 visits** (was 61 → 297), and **48 after switching through 6 parts** (was 449)._ |
| D17 | ✅ | **The AI suggestion chips ask things no stored answer covers.** "Reset Hookup" (RESET pin) gets the "be more specific" reply, "555 Astable Eq" gets the NE555 card without the timing equation, and "ESP32 5V Tolerance" gets the ESP32 overview, which doesn't mention 5V. Fix by writing 3 answers or changing the chips. _Found in #12._ _**Fixed in #12b:** the chips keep their questions and get three new stored answers, each checked against the official datasheet: the RESET pin (active LOW, VRST 0.2–0.9 VCC, 2.5 µs minimum pulse, 30–60 kΩ internal pull-up, the 100 nF auto-reset cap and the RSTDISBL warning) from the Microchip ATmega328/P datasheet; the NE555 astable equations (tH, tL, T, f = 1.44 / ((RA + 2RB) × C) and the duty cycle) from the TI NE555 datasheet (SLFS022); and ESP32 5 V tolerance (**not** tolerant, 3.6 V absolute maximum, VIH 0.75 × VDD, a divider for slow signals, a level shifter for I2C) from the Espressif ESP32 Series Datasheet. The matcher also changed: a topic marked `specific` is checked **before** the part card when 2 of its keywords hit, so "the timing equation for NE555 Astable Mode" gives the equation instead of the NE555 spec card. Asking about the part itself still gives the card (tested)._ | [index.html:495-497](../../index.html), [data.js:827](../../src/data/) |
| D18 | ✅ | **AI replies show lists and tables as raw text.** The formatter only turns lines starting with "•" into a list, but the stored answers use "- " (43 lines, 0 list items). Tables (20 rows) show as raw `\|` pipes. Still readable. _Found in #13._ _Re-measured in #13b: 45 "- " lines and 34 table rows (5 tables) across the 9 stored answers, after #12b added three more; the page rendered **0** `<li>`, **0** `<ul>` and **0** `<table>`, and one reply showed 18 raw pipes. The `•` rule was dead code — no stored answer uses that character._ _**Fixed in #13b:** the formatter walks the text line by line. A run of "- " (or "• ") lines becomes one `<ul>`, and a `|---|` separator turns the rows around it into a real `<table>`. Each block is set aside and put back last, the same trick the code blocks already used (D7), so a list or table can never swallow the headings between them. Tables are styled with the design tokens in `views/ai.css`. Also fixed on the way: the old `(<li>.*</li>)/s` rule would have wrapped everything between the first and last item in a single `<ul>`._ | [app.js:1754](../../src/app/app.js) |
| D19 | ✅ | **Deleting a part (right-click) keeps one of its wires.** `onContextMenu` removes the part, then filters the wires using `components[i]`, which is now the *next* part. Measured: battery–LED–resistor with 2 wires, delete the LED → 1 wire left, still pointing at the deleted LED (should be 0). _Found in #14._ _**Fixed in #14b:** the removed part is kept and its wires are filtered by it (tested with a real right-click)._ | [simulator.js:768](../../src/engines/simulator/index.js) |
| D20 | ✅ | **The simulation clock counts frames, not real time.** Each frame adds 0.016 s × speed, so the clock runs slow when the browser draws slowly (for example in headless tests). Other engine animations are time-based. _Found in #14._ _Measured in #15: 0.51 simulated seconds per real second at 33 frames/s, about 0.3 under test load._ _**Fixed in #15:** the clock adds the real time since the last frame (at most 1 s), and so does the wire animation. Now 1.00× (3.03× at speed 3), and 0.96–0.99× under test load (tested)._ | [simulator.js:955](../../src/engines/simulator/index.js) |
| D21 | ✅ | **The oscilloscope shows made-up signals.** CH1 is a 5 V sine from a signal generator that isn't connected to the circuit, and it runs at 1 Hz while the label says "1.0kHz" (the time in seconds is divided by 1000). CH2 is just "3.3 V if any LED is on". The multimeter's Resistance mode also just adds up every resistor on the board, wired or not. The V/div and T/div dials (C7) can only make sense once the scope shows real circuit values. _Found in #14b._ _**Fixed in #14c:** CH1 is the battery's voltage and CH2 the voltage across the first LED, both from the solver (measured: 9.00 V, and 0 V → 2.07 V when a switch closes). Samples keep their simulation time and the screen shows the last 10 × T/div, 0 V on the centre line. The scope screen draws at its real size. Resistance mode shows the resistance the battery sees (V ÷ I), or OL when no current flows. The unused signal generator (`setSigGen`) is removed. Also fixed: a part added by drag and drop now runs the solver (found by the tests: the new scope label crashed on it)._ | [simulator.js:1056](../../src/engines/simulator/index.js), [simulator.js:1140](../../src/engines/simulator/index.js), [app.js:934](../../src/app/app.js) |
| D22 | ✅ | **The Board Explorer adds its canvas mouse handlers again on every visit.** They are anonymous functions, so they pile up: after 3 visits one pin click shows 3 toasts. The board is also not redrawn when the window is resized (read in the code); that needs the handlers set up once first. _Found in #16._ _**Fixed in #16b:** the handlers and a window-resize listener are added on the first visit only, and a new `sizeBoardCanvas()` sizes and redraws the board on each render and on resize (tested: 1 toast per click after 3 visits; the drawing follows a resized window, before 862×529 on a 682×459 board)._ | [app.js:971](../../src/app/app.js), [app.js:1103](../../src/app/app.js) |
| D23 | ✅ | **A clicked board pin is not highlighted in the pin list.** `selectBoardPin` removes the `selected` class from every row but never adds it back. _Found in #16._ _**Fixed in #16b:** each row carries its pin number, and a click highlights the matching row (tested). The unused `detail` variable is removed._ | [app.js:1135](../../src/app/app.js) |
| D24 | ✅ | **3 of the 7 board tabs show a different board.** Only 4 boards exist in the data: the Mega tab shows the Uno, ESP8266 shows the ESP32, and RPi Pico shows the Pi 4; "STM32 Nucleo" shows a Blue Pill. Fix by adding the missing boards' data or removing those tabs (a choice for the owner). _Found in #16._ _**Decided in #16b:** add real data for the Mega, ESP8266 and Pico, in its own branch (#16c)._ _**Fixed in #16c:** real pin data for the Arduino Mega 2560 Rev3 (85 pins), NodeMCU 1.0 / ESP8266 (28), Raspberry Pi Pico (40) and STM32 Nucleo-F401RE (68, ST morpho headers), and an 8th "STM32 Blue Pill" tab for the existing Blue Pill data (the owner chose to keep both). Every pin comes from official sources, listed in a comment above each board in `data.js`: Arduino's pinout PDF and AVR core; the NodeMCU devkit pin map, firmware docs and ESP8266 core; Raspberry Pi's Pico pinout, docs and SDK; ST's Nucleo pinout images and stm32duino core. One conflict: the NodeMCU repo's pin-list text says D1 = GPIO4, D2 = GPIO5, but its pin-map image, the firmware docs and the ESP8266 core all say D1 = GPIO5, D2 = GPIO4, which is used. Tested: every tab shows its own board, all pin data is well-formed with no overlapping pads, and key pins match the official pinouts._ | [app.js:960](../../src/app/app.js), [data.js:518](../../src/data/) |
| D25 | ✅ | **The Board Explorer and the 3D Viewer shared one selected pin.** Both used `state.selectedPin`, so clicking Uno pin 5 in the Board Explorer made the 3D Viewer select ATmega328P pin 5, and switching boards kept pin 5 selected (ESP32's GPIO21 was highlighted though nobody clicked it). _Found and fixed in #16b:_ the Board Explorer has its own `state.selectedBoardPin`, cleared when the board changes (tested). | [app.js:15](../../src/app/app.js) |
| D26 | ✅ | **The Datasheet "Pinout" section has no data**, so clicking it shows the placeholder "Section content under catalog index division." Meanwhile a `package` section **does** have data but has no button. The buttons are Overview, Pinout, Electrical, Timing, Memory, Examples; the data sections are overview, electrical, examples, **package**, timing, memory. _Measured in #17: 5 of the 6 buttons show content, "Pinout" shows the placeholder._ Fix by writing a pinout section for both datasheets, or by swapping the button to "Package". _**Fixed in #17c, without inventing any data:** the Pinout section is drawn from the linked component's own `pinout` list — the same 28 pins (ATmega328P) and 38 pins (ESP32-WROOM-32) the 3D Viewer's pin table already uses — showing pin number, name, alt name, type, voltage and alt functions. A 7th **Package** button now shows the `package` data that was already stored (DIP-28, TQFP-32, MLF-32 with their sizes). Both use the `.ds-table` styles that already exist. Tested: all 7 buttons show a real section, none shows a placeholder._ | [index.html:470](../../index.html), [data.js:811](../../src/data/) |

| D27 | ✅ | **The Board Explorer's redraw-on-resize measured the board too early.** The `resize` handler read `getBoundingClientRect()` while the event was still firing, so it could see the old box and size the drawing to it; nothing measured again afterwards. It also assigned the fractional box straight to `canvas.width`/`height`, which truncates. _Measured in #12b: the "board redraws at its new size" test failed 3 times in 60 runs with 4 browsers — `box=[682,459] buffer=[682,458]`, exactly 1 px short on the height, while the width matched. Left alone it fails a `--repeat-each=4` run often enough to block a merge._ _Part of D22's fix in #16b, missed there._ _**Fixed in #12b:** the handler waits one animation frame before measuring, and the size is rounded rather than truncated. 60 of 60 runs green after the fix, 57 of 60 before._ | [app.js:982](../../src/app/app.js), [app.js:1108](../../src/app/app.js) |
| D28 | ✅ | **The background ignores the theme.** `circuit-bg.js` checks `document.body.classList.contains('dark-theme')`, but no code ever sets that class: the theme is `data-theme="light"` on `<html>` (C5), and dark is the default with no attribute. So `isDark` is always `false` and the **light** colours are used in both themes; in dark mode the grid lines are light grey (`rgba(210,215,220,0.4)`). _Found in #21 (checked: `dark-theme` appears nowhere else in `src/` or `index.html`; seen on a dark-mode screenshot)._ _Measured in #22c: the grid was `rgba(210, 215, 220, 0.4)` in both themes._ _**Fixed in #22c:** the check reads `data-theme` on `<html>`, every frame, so a theme switch changes the background at once. Dark mode now draws `rgba(48, 54, 61, 0.25)` lines — faint but visible, checked on screenshots of both themes (tested)._ | [circuit-bg.js:104](../../src/engines/background/circuit-bg.js) |
| D29 | ✅ | **Background electrons keep running on the old grid after a window resize.** `resize()` builds a new node list but does not respawn the particles, which still hold references to the old nodes, so they travel along traces that are no longer drawn (and stay inside the old window size). The electrons also move per frame, not per second, and `shadowBlur` on every particle is costly on the CPU. _Found in #21 (read in the code, not yet measured)._ _Measured in #22c: after resizing 800 → 1600 px, 2215 of 2400 dot positions (92%) were off the drawn grid, still 82% 5.5 s later._ _**Fixed in #22c:** `resize()` starts the particles again on the new grid (tested: under 5% off, 85% before). **Left open:** the speed is still per frame, and `shadowBlur` is still drawn per particle — a reliable test for frame-rate-independent speed needs a fake animation clock. Pausing while the tab is hidden needs nothing: browsers already stop animation frames in hidden tabs._ _**Speed follow-up in #28:** the dots now move by real time (progress × frames elapsed at 60 Hz, at most 4), so a slow frame no longer slows them. `shadowBlur` per dot is still drawn._ | [circuit-bg.js:26-32](../../src/engines/background/circuit-bg.js) |
| D30 | ✅ | **Two ICs are filed as passive parts.** The NE555 (timer) and LM358 (op-amp) have `category: 'passive'`, so the Database's PASSIVE filter lists them next to the resistor, capacitor and LED, and there is no IC category. _Found in #23b while checking the PASSIVE filter (it shows 5 parts: the 3 new ones, LM358 and NE555)._ Fix by adding an `ic` category to the data and the filter. | [data.js:122](../../src/data/), [data.js:188](../../src/data/), [app.js](../../src/app/app.js) _**Fixed in #28:** both are `category: 'ic'`, the library has an IC filter, and `.type-ic` badges use the design tokens (tested: IC shows LM358 and NE555, PASSIVE shows neither)._ |
| D31 | ✅ | **A typed project name could run code.** `showToast()` put its message into the page as HTML, and the "Created …" toast passes the name straight in, so naming a project `<img src=x onerror=…>` ran it. The ✕ on your own cards also wrote the stored id into an `onclick`, so an id edited by hand in the browser's storage ran as code. _Found in #31._ _**Fixed in #31:** a toast sets its text with `textContent`, so a message is never HTML, and the cards carry their id in `data-id`, read by one click handler on the grid. Two tests: a name with HTML shows as text, and a hand-edited id just deletes its project._ | [src/ui/toast.js](../../src/ui/toast.js), [src/views/projects.view.js](../../src/views/projects.view.js) |
| D32 | ✅ | **The 3D Viewer's buttons did not match the engine.** "Wire" looked for `btn-wireframe`, but the button's id is `btn-wire`, so clicking Wire left no button marked (and cleared the camera buttons too). The sidebar is drawn again on every visit and part switch, always showing Solid and Auto Rotate on, while the engine kept its real setting; `W` and `E` changed the engine without touching the buttons; and a new part was drawn solid though the engine still said wireframe. _Found in #31._ _**Fixed in #31:** one `syncViewerControls()` marks the mode the engine is really in and ticks Auto Rotate from `isAutoRotating()`; the buttons carry `data-mode`, so the camera buttons are left alone; `W`/`E` go through `setViewMode()`; and a new model is drawn in the mode that is on. Tested._ | [src/views/viewer.view.js](../../src/views/viewer.view.js) |
| D33 | ✅ | **Old pin details stayed on screen after switching part.** Switching from ATmega328P pin 5 to the NE555 left the ATmega pin in the detail panel, and the new part's pin 5 was marked in the table though nobody clicked it. _Found in #31._ _**Fixed in #31:** switching part clears the selected pin and the panel (tested)._ | [src/views/viewer.view.js](../../src/views/viewer.view.js) |
| D34 | ✅ | **A 3D pin could stay cyan for good.** Hovering a pin's table row highlighted it, and clicking the row highlighted it again — the second time saving the highlight copy as the pin's "own" material, so putting it back restored the highlight. The real material was lost, and every hover left a material copy behind. _Found in #31._ _**Fixed in #31:** a pin already highlighted is not highlighted again, and putting it back frees the copy. A new `getModelInfo().highlighted` lists the highlighted pins, and a test hovers, clicks and hovers away._ | [src/engines/three-viewer/index.js](../../src/engines/three-viewer/index.js) |
| D35 | ✅ | **Hiding the sidebar left the canvases the wrong size.** The 3D Viewer, the Simulator board and the Board Explorer only listened for a **window** resize, but the sidebar button changes their width with no window event. The Simulator drawing was then stretched while clicks used screen pixels, so pins near the right edge could not be clicked. _Found in #31._ _**Fixed in #31:** each canvas watches its own box with a `ResizeObserver`, which covers the window, the sidebar and a screen shown again after a resize. Three tests (Viewer, Simulator, Boards)._ | [three-viewer/index.js](../../src/engines/three-viewer/index.js), [simulator/index.js](../../src/engines/simulator/index.js), [boards.view.js](../../src/views/boards.view.js) |
| D36 | ✅ | **The Datasheet screen could show one part and explain another.** Only 2 datasheets exist, so a part without one fell back to the ATmega328P datasheet: the header said ATmega328P, no row was marked in the list, and "AI Explain" asked about the selected part instead ("NE555 microcontroller"). _Found in #31._ _**Fixed in #31:** one `shownDatasheet()` decides what is on screen, and the header, the marked row, "AI Explain" and "Download PDF" all use it (tested)._ | [src/views/datasheet.view.js](../../src/views/datasheet.view.js) |
| D37 | ✅ | **Search closed itself if reopened quickly.** Closing hides the pop-up 200 ms later, and that timer was never cancelled, so pressing Esc and then Ctrl+K within 200 ms left you typing into a pop-up that then vanished. _Found in #31._ _**Fixed in #31:** opening cancels the pending hide (tested)._ | [src/ui/search.js](../../src/ui/search.js) |
| D38 | ✅ | **Two quick questions could be answered in the wrong order.** Each reply waited 600–1000 ms at random, so a later question could be answered first. _Found in #31._ _**Fixed in #31:** the waits are chained, so replies always follow the questions (tested with the waits forced the wrong way round)._ | [src/views/ai.view.js](../../src/views/ai.view.js) |
| D39 | ✅ | **The nRF24L01+ was filed as a microcontroller**, so it showed under the MCU filter though it is a 2.4 GHz transceiver with no CPU to program. _Found in #31._ _**Fixed in #31:** its category is `ic`, like the other ICs (D30). Tested._ | [src/data/components.js](../../src/data/components.js) |
| D40 | ✅ | A part with no pin list showed its message across 6 of the pin table's **7** columns. _Found in #31._ _**Fixed in #31** (tested against the real number of columns)._ | [src/views/viewer.view.js](../../src/views/viewer.view.js) |
| D41 | ✅ | **A part could get stuck to the mouse.** `mouseup` was only listened for on the board, so letting go over the palette left the part following the mouse when it came back. Wires also only jumped into place when a part was dropped, instead of following it. _Found in #31._ _**Fixed in #31:** `mouseup` is listened for on the window, and wires follow a part while it moves (tested with a drag released outside the board)._ | [src/engines/simulator/index.js](../../src/engines/simulator/index.js) |
| D42 | ✅ | **The 3D model turned at the frame rate, not by time.** The camera moved a fixed step per frame, so the model spun about 2.4× faster on a 144 Hz screen than at 60 Hz and crawled in the tests — against the project's rule that engine animations are time-based. _Found in #31._ _**Fixed in #31:** the turn and the camera easing use the real time since the last frame (0.18 rad/s). Tested: 0.14–0.22 rad/s whatever the frame rate._ | [src/engines/three-viewer/index.js](../../src/engines/three-viewer/index.js) |
| D43 | ✅ | **Your projects said "just now" for ever**, because that text was stored instead of a time. _Found in #31._ _**Fixed in #31:** the time it was made is stored and the card works out "3 days ago" (tested). Projects saved before this keep the text they were saved with._ | [src/views/projects.view.js](../../src/views/projects.view.js) |
| D44 | ✅ | **Export forgot whether a switch was closed**, so a circuit loaded back did not work the same, and the download's file link was never freed. _Found in #31._ _**Fixed in #31:** a switch saves its position, `loadCircuit()` restores it, and the link is freed after the download (tested: export a closed switch, clear, load — the LED is still lit)._ | [src/engines/simulator/index.js](../../src/engines/simulator/index.js) |
| D45 | ✅ | **Blurry drawings on a high-DPI screen.** The Simulator board, the oscilloscope, the Board Explorer and the background all drew one pixel per CSS pixel, so everything was soft on a 2× screen. _Found in #31._ _**Fixed in #31:** each canvas takes `devicePixelRatio` pixels per CSS pixel and scales its drawing, so the mouse still works in screen pixels (tested at 2×: sharp, and clicks still hit their part and pin). The 3D Viewer already did this, and now reads the ratio again on every resize._ | [simulator/index.js](../../src/engines/simulator/index.js), [boards.view.js](../../src/views/boards.view.js), [circuit-bg.js](../../src/engines/background/circuit-bg.js) |
| D46 | ✅ | **Search only found components**, though its box says "Search components, boards, datasheets…". _Found in #31._ _**Fixed in #31:** it searches all three, marks which is which, and opens the right screen (tested: a board tab and a datasheet)._ | [src/ui/search.js](../../src/ui/search.js) |
| D47 | ✅ | **"Open →" on your own project opened an empty Simulator** and nothing was kept, so a project was only a name. _Found in #31._ _**Fixed in #31:** opening a project loads its circuit, and every change to the board is saved into the project it belongs to (tested: add a battery, reload, open again)._ | [src/views/projects.view.js](../../src/views/projects.view.js), [src/engines/simulator/index.js](../../src/engines/simulator/index.js) |
| D48 | ✅ | **"Download PDF" only showed a made-up message** ("Initiating secure technical PDF assembly boundary..."). _Found in #31._ _**Fixed in #31:** each datasheet stores its maker's official PDF (Microchip DS40002061B, Espressif ESP32-WROOM-32 v3.2), and the button opens the one on screen (tested)._ | [src/data/datasheets.js](../../src/data/datasheets.js) |
| D49 | ✅ | **"Open →" on a sample project opened an empty Simulator**, or one still holding your own project, and your edits kept being saved into it. _Found in #32._ _**Fixed in #32:** a sample project has no circuit, so its button is now "View in 3D →" and shows its main part in the 3D Viewer; your project is never touched (tested)._ | [src/views/projects.view.js](../../src/views/projects.view.js) |
| D50 | ✅ | **4 of the 8 boards had short pin lists with no source:** Uno 23 pins, ESP32 DevKit 24 of 30, Pi 4 26 of 40, Blue Pill 24 of 40. _Found in #32._ _**Fixed in #32:** every header pin is listed (Uno 31, ESP32 30, Pi 4 40, Blue Pill 40), with a "Sources:" comment above each board like the other four. The ESP32, Pi 4 and Blue Pill lists were checked against their sources; **the Uno list follows the standard shield layout and still needs a check against Arduino's official pinout** (tested: the counts, and no overlapping pads)._ | [src/data/boards.js](../../src/data/boards.js) |
| D51 | ✅ | **The ESP32 3D model's pin headers were turned 90°:** the black strips ran across the board, 2.8 deep on a 1.1-deep board, while their pins ran along it. _Found in #32._ _**Fixed in #32:** the strips run along their pins and stay on the board (tested with the model's size)._ | [models/boards.js](../../src/engines/three-viewer/models/boards.js) |
| D52 | ✅ | **Any question with "difference", "compare" or "vs" got the SPI vs I2C answer**, e.g. "what is the difference between a diode and an LED". _Found in #32._ _**Fixed in #32:** those words are no longer keywords; "spi vs i2c" still gets the SPI answer (tested)._ | [src/services/ai.js](../../src/services/ai.js) |
| D53 | ✅ | **The Dashboard's sample project cards looked clickable but did nothing.** _Found in #32._ _**Fixed in #32:** a click opens the project like its "View in 3D →" button (tested)._ | [src/views/dashboard.view.js](../../src/views/dashboard.view.js) |
| D54 | ✅ | **The sample projects had made-up data:** fixed "2 days ago" times (like E5), the Weather Station listed an MPU-6050 though it uses a BME280, and the Robot Arm said Arduino Mega though it lists the Uno's ATmega328P. _Found in #32._ _**Fixed in #32:** the times are removed and both are corrected (tested)._ | [src/data/projects.js](../../src/data/projects.js) |

### E. Cleanup / project health

| # | Sev | Problem |
|---|---|---|
| E1 | ✅ | **Two versions of the app are mixed together.** `legacy/script.js` (old app) and `legacy/database.js` (old data) aren't used by the new app. `script.js` would also crash if loaded: it imports `ThreeViewer` / `CircuitSimulator`, which those files don't export. **Fixed in #6:** `legacy/` deleted, and the build output is byte-identical before and after. **Old data worth restoring later** (in a different format from `data.js`; restore with `git show 3cd518a:legacy/database.js`): resistor and capacitor entries (for D8's unused models), a description for every Arduino Uno pin, and datasheet electrical tables for 5 parts (Arduino Uno, ESP32, NE555, resistor, capacitor). The old wiring code is at `git show 3cd518a:legacy/script.js`. _**Resistor and capacitor added in #23b, but not from the old data:** its numbers had no source and its text was filler ("vacuum-deposition techniques on premium ceramic core substrates"), so the new entries are real parts with every value from the manufacturer (YAGEO CFR-25, Panasonic ECA-1EM101), plus a Kingbright WP7113ID LED. The old pin descriptions and datasheet tables are still only in git history._ |
| E2 | ✅ | `manifest.json` is linked in the HTML but doesn't exist (browser logs a syntax error). `favicon.ico` is missing (404). _**Fixed in #25:** a real `public/manifest.json` (name, icons, colours, standalone display) and an SVG `public/favicon.svg`, linked from the page. Both 404s were hidden by `IGNORED_CONSOLE_ERRORS`; those two entries are removed and a test now asserts **nothing the page requests returns 4xx**._ |
| E3 | ✅ | CSS is loaded twice: `<link>` in [index.html:28](../../index.html) **and** `import` in [src/main.js](../../src/main.js). _**Fixed in #25:** the `<link>` is removed; `src/main.js` imports it, which is also what puts it through the build. A test asserts the page links no `main.css`._ |
| E4 | ✅ | `assets/fonts`, `assets/models`, `assets/icons` are empty folders. _Re-checked in #25: the whole `assets/` tree had already gone in the restructure. The one empty folder left was `public/icons` (just a `.gitkeep`), now removed — `public/` holds the real favicon and manifest instead._ |
| E5 | ✅ | The notifications panel shows fake hardcoded messages ("Just now", "3 mins ago"). _**Fixed in #25:** the feed is built at startup from what actually loaded — the real component, board, datasheet and project counts — and timestamped with the real start time instead of invented "3 mins ago" values. A test checks the text quotes the true counts._ |
| E6 | ✅ | `npm audit`: 3 known security issues in dev tools — `nanoid` (high), `postcss` (high), `esbuild`/`vite 5.4.21` (moderate). These only affect the dev machine, not visitors. _Measured in #26: 4 issues (1 moderate, 3 high). `npm audit fix` cleared `nanoid` and `postcss`; the `esbuild` one (any website could send requests to the running dev server and read the replies) needed a new Vite major._ _**Fixed in #26:** `npm audit fix`, then **Vite 5.4.21 → 8.3.0** (needs Node `^20.19.0` or `>=22.12.0`). `npm audit` now finds **0 vulnerabilities**. One config change: `minify: 'esbuild'` was removed, because Vite 8 no longer ships esbuild and the build failed with it; Vite 8's default minifier is used. Build 1.35 s → 0.25 s; JS 687.9 kB (179.2 kB gzipped) → 694.5 kB (178.9 kB gzipped); CSS 52.9 → 52.1 kB. Checked: the production build opens in `vite preview` with the 3D model and no errors, and every smoke test passes._ |
| E7 | ✅ | Three.js **r128** (from 2021) is loaded from a CDN. It's old, and the app needs the internet to work. _Measured in #24: with every outside request blocked, opening the Viewer gave `THREE is not defined` and the 3D engine never started. The global `three.min.js` build no longer exists (removed in r161), so the fix had to be an import._ _**Fixed in #24:** Three.js **0.186.0 (r186)** from npm, pinned, imported by the 3D engine and bundled by Vite; the CDN `<script>`, the `window.THREE` check and the ESLint global are gone. Changes needed for r129–r186 ([migration guide](https://github.com/mrdoob/three.js/wiki/Migration-Guide)): lights are physically correct since r155, so every intensity is × π and point lights use decay 1, as the guide advises (screenshots before and after match); `PCFSoftShadowMap` (deprecated in r181) → `PCFShadowMap`; the chip-label canvas texture is marked sRGB (colour management since r152). Tested: no request to a CDN, and the Viewer shows a model with the internet blocked, with no Three.js warnings. The bundle is now 688 kB (179 kB gzipped) instead of 162 kB plus ~600 kB from the CDN (E18)._ |
| E8 | ✅ | No git, no README, no linter, no tests. _Fixed: git, README, smoke tests (`npm run test:smoke`) and ESLint (`npm run lint`) added._ |
| E9 | ✅ | `dist/` is a build of the broken code. Rebuild it after fixing. _**Closed in #30:** `dist/` is in `.gitignore` and is never committed; `npm run build` makes a fresh one from the current code (and CI or a deploy should do the same)._ |
| E10 | ✅ | **GSAP and ScrollTrigger are downloaded on every page load but never used** by any code. That's wasted network and load time. _Found while setting up ESLint._ _**Fixed in #25:** both script tags removed (nothing in `src/` mentions GSAP). **FontAwesome went too** — its whole CDN stylesheet was loaded for exactly one glyph, the robot in the AI header, now a 🤖 character. Three CDN requests gone from every page load; tests assert neither is fetched._ |
| E11 | ✅ | **27 lint warnings** (25 after #4, 24 after #10, 18 after #14b, 17 after #15, 15 after #16b, 13 after #23, 12 after #21, 9 after #23b, 7 after #25b, 0 after #28) (unused code, an empty `catch`, `const` in `switch` cases), capped with `--max-warnings` (currently 0). Most are symptoms of D5, D8, D10–D12. Each fix branch clears its own warnings and lowers the cap. _**Fixed in #28: 0 warnings, cap 7 → 0.** Removed the unused `controls`, `animationId`, `selectedComponent`, `h`, `dy` and `cols`, and `buildArduinoUno` with `addTraces` (used only by it; the Uno is a board, not a component)._ |
| E12 | ✅ | **Unused Simulator CSS in `main.css`.** Rules for IDs that don't exist in the page (`#sim-toolbar`, `#sim-palette`, `#sim-canvas-area`, `#sim-instruments`, `#sim-osc-panel`, `#osc-main-canvas`) and classes nothing uses (`.sim-status-dot`, `.palette-section-title`, `.sim-canvas-hint`…). Harmless; the working styles are in `src/styles/views/simulator.css` since #14. _Found in #14._ _Also found in #17: the same in the Datasheet block — `.datasheet-toc`, `.toc-header`, `.toc-title`, `.toc-meta`, `.toc-list`, `.toc-item`, `.toc-num`, `.toc-actions` and `.datasheet-content` ([main.css:1735-1782](../../src/styles/)) style the new design's markup, which this page doesn't use. The working styles are in `src/styles/views/datasheet.css` since #17._ _**Fixed in #25:** 29 dead rules removed, `main.css` 2267 → 2072 lines (−195). Every selector was checked against the **live app** first — all 22 matched 0 elements after visiting all 9 screens. The built CSS drops 54.76 kB → 52.97 kB._ |
| E13 | ✅ | **A flaky smoke test.** `boards.spec.js` "visiting the Board Explorer again does not repeat pin clicks (D22)" counted **all** toasts before the click and expected one more. Toasts delete themselves after 3.3 s ([app.js:1710-1713](../../src/app/app.js)) and the app shows a welcome toast at 800 ms ([app.js:86](../../src/app/app.js)), so under load an old toast expired as the new one appeared and the count never rose. _Measured in #17: 1 failure in 12 runs, and it failed in a `--repeat-each=4` run, blocking the merge gate._ _**Fixed in #17:** the test counts only the toast this click makes (`.toast` filtered by "Inspecting Pin"). Proved it still catches D22 on a scratch copy with the handler re-added on every visit: 3 visits → 3 identical toasts, so the count is never 1 and the test fails. 12 of 12 runs green after the fix._ | [boards.spec.js:84](../../tests/smoke/boards.spec.js) |
| E14 | ✅ | **A network hiccup can fail any smoke test.** `index.html` loads its fonts from `fonts.googleapis.com`, and the test fixture fails on any `console.error`, so a transient CDN failure fails whichever test is running. _Found in #17:_ `console.error: Failed to load resource: net::ERR_CERT_VERIFIER_CHANGED (https://fonts.googleapis.com/css2?family=Syne...)` failed `viewer.spec.js` "view mode buttons switch wireframe". Fix by self-hosting the fonts, or by adding the font CDN to `IGNORED_CONSOLE_ERRORS` tagged E14. | [index.html](../../index.html), [helpers.js:7-10](../../tests/smoke/helpers.js) _**Fixed in #25:** the font CDNs are the one entry in `IGNORED_CONSOLE_ERRORS`, tagged E14, because a hiccup at Google is not a bug in this app. A test blocks `fonts.googleapis.com` and `fonts.gstatic.com` outright and checks the app still starts and reports no errors. Self-hosting the three families would remove the dependency entirely, but that is a bigger change than this branch._ |

| E15 | ✅ | **Every smoke test waits for the 3D engine, even screens with no 3D.** `openApp()` in `helpers.js` waited for `ThreeViewer.isReady()`, so all tests blocked on software-rendered WebGL starting up, not just the Viewer ones. CLAUDE.md said "only Viewer tests pay this cost", which was not true. _Found in #18: the suite grew to 516 slots under `--repeat-each=4` and 4 tests timed out at 30 s with no assertion failing — 3 of them on the Projects screen, which shows no 3D. The timeout was raised to 60 s as a stop-gap._ _**Corrected in #18b:** the #18 note claimed the waiting was "the real cost" of the suite. **That was wrong, and it was never measured.** Removing the wait changed `npm test` from 160.9 s to 158.2 s — about 1.7%, which is noise. The suite is dominated by the 3D tests rendering, not by other tests waiting ~300 ms for the engine: `viewer.spec.js` runs 9 tests in 25.4 s (~2.8 s each) while `projects.spec.js` runs 7 in 8.4 s (~1.2 s each)._ _**Fixed in #18b anyway, for the real reason:** a Projects or Settings test should not fail because WebGL is slow. `openApp()` now waits only for `CircuitApp` and `CircuitLabData`; `waitForViewer()` waits for the engine and `openViewer()` calls it; `modelSize()` copes with the engine not existing yet, so tests that reach the Viewer by navigating still work. The timeout is back to **30 s**: 572 slots now pass clean at 30 s, where 516 slots failed 4 at 30 s in #18._ | [helpers.js:59-83](../../tests/smoke/helpers.js), [playwright.config.js:9](../../playwright.config.js) |
| E16 | ✅ | **Leftovers nothing uses.** `<div id="particle-field">` in `index.html` is empty and no code or CSS touches it. `loadProjects()` reads `circuitlab-projects` into `state.projects`, which nothing reads (the C4 projects use `circuitlab.my-projects`); its empty `catch` is one of the lint warnings. _Found in #22 while reading the code._ _**Fixed in #25b:** the div, `state.projects` and `loadProjects()` are removed (checked first: nothing else in `src/`, `index.html` or the tests used them). The empty `catch` went with it, so 2 lint warnings fewer (cap 9 → 7). Tested: no `#particle-field`, no `projects` in the app state, and `app.js` no longer mentions `circuitlab-projects`._ | [index.html:29](../../index.html), [app.js](../../src/app/app.js) |
| E17 | ✅ | **A slow 3D test can run out of time under load.** `viewer-models.spec.js` "visiting the Viewer again does not leak graphics memory (D16)" rebuilds the 3D model 5 times in software WebGL, which takes about 17 s on its own, close to the 30 s test limit. _Found in #22d: it timed out once in the 676-run `--repeat-each=4` gate ("the model is rebuilt", test timeout of 30000ms exceeded), blocking the merge. Re-run on its own with `viewer.spec.js`, `--repeat-each=6`: 90 of 90 passed, so it is load, not a bug in the app._ _**Fixed in #22d:** that one test is marked `test.slow()` (3× its time limit); the global 30 s limit is unchanged._ | [viewer-models.spec.js:59](../../tests/smoke/viewer-models.spec.js) |
| E18 | ✅ | **One 688 kB JavaScript bundle.** Since #24, Three.js (~530 kB) is bundled with the app, so every screen waited for the 3D library although only the Viewer uses it, and `npm run build` warned "Some chunks are larger than 500 kB". _Found in #24._ _**Fixed in #31:** `views/viewer.view.js` loads the 3D engine with a dynamic `import()` the first time the Viewer opens, so Vite gives Three.js its own file. The app is **146 kB (43 kB gzipped)** instead of 693 kB (179 kB), and the 550 kB 3D file is only downloaded for the Viewer. `chunkSizeWarningLimit` is 600 kB, so the build warns about anything bigger than that one deliberate chunk. Tested: opening the other 8 screens asks for nothing with "three" in its address; the Viewer does, and still shows a model offline._ | [vite.config.js](../../vite.config.js), [viewer.view.js](../../src/views/viewer.view.js) |
| E19 | ✅ | **The web fonts were requested twice:** `index.html` links Syne, JetBrains Mono and Inter, and the stylesheet `@import`ed JetBrains Mono and Inter again. _Found in #27 planning._ _**Fixed in #28:** one `<link>` in `index.html`, now with the 700 weights the `@import` added; `base/fonts.css` is removed (tested: 1 font CSS request)._ | [index.html:17](../../index.html) |
| E20 | ✅ | **Dead code and made-up numbers.** About 90 CSS class rules matched nothing (most of the old element-based board drawing, "full" project cards, a signal generator, header leftovers); `state.searchQuery` was never set, so the Library's search filter never ran; the Simulator's "Upload Code" and "NE555 IC" buttons could only say "not available"; a hidden "Astable Multivibrator" card sat in `#projects-grid`, replaced before anyone saw it; the Dashboard showed a fixed "Protocols: 12" and a stat bar whose width meant nothing; the Datasheet showed placeholder text for a missing section. _Found in #31._ _**Fixed in #31:** all of it removed or made real — the Protocols stat counts the protocols the screen lists, the bars compare the four numbers, a missing section says "<name> has no <section> section", and a new test keeps every CSS class in use._ | `src/styles/`, [index.html](../../index.html) |
| E21 | ✅ | Empty folders with only a `.gitkeep` were still in the tree (`src/assets/`, `scripts/`, `tests/unit/`), though E4 said they were gone. _Found in #31._ _**Fixed in #31:** removed, and the docs no longer list them. Add such a folder again when something real goes in it._ | — |
| E22 | ✅ | **46 links in this file pointed past the end of their file**, because they still carried line numbers from `app.js` before it was split (for example `app.js#L1754`, in a file that now has 99 lines). _Found in #31._ _**Fixed in #31:** those links point at the file, and a test now checks every link in every doc._ | [docs/FIX_PLAN.md](FIX_PLAN.md) |
| E23 | ✅ | **8 Simulator part types could never be placed:** the palette had only 4 parts and there is no Import button. _Found in #32._ _**Fixed in #32:** the switch, diode and ground, which the solver really simulates, get palette buttons; the transistor, op-amp, MOSFET, Arduino and ESP32, which it could only draw, are removed (−259 lines, tested)._ | [simulator/index.js](../../src/engines/simulator/index.js), [index.html](../../index.html) |
| E24 | ✅ | **Unused code:** 8 functions exported but never imported, `ThreeViewer.onResize` and `CircuitSimulator.getCircuit` in the public API with no outside user, 5 unused 3D materials, the Simulator's unused purple, a fake "JD" user avatar with no login behind it, and `.gitkeep` files in folders that are no longer empty. _Found in #32._ _**Fixed in #32:** all removed._ | `src/`, [index.html](../../index.html) |
| E25 | ✅ | **Old comments and misleading text:** comments still named `main.css`, `eslint.config.js` still said to "lower the cap" (it is 0 for good), the AI screen said "Powered by CircuitLab AI — Ask anything" though its answers are stored, and the Settings label read "Contrast Dark System Mode". _Found in #32._ _**Fixed in #32:** reworded to say what is really there._ | [index.html](../../index.html), [eslint.config.js](../../eslint.config.js) |
| E26 | ✅ | **A service showed a message:** `services/my-projects.js` imported `ui/toast.js`, against the rules in ARCHITECTURE. _Found in #32._ _**Fixed in #32:** `saveMyProjects()` returns false when the browser won't save, and the Projects screen shows the message._ | [src/services/my-projects.js](../../src/services/my-projects.js) |
| E27 | ✅ | **Stale docs:** 30 `#L` line links in this plan landed on the wrong lines (the link test only checks the line exists), the LEARNING_GUIDE still described every file as a `window` IIFE and answers "from `data.js`", the ARCHITECTURE test tree and import rules didn't match the code, and the CHANGELOG repeated its sections. _Found in #32._ _**Fixed in #32:** the line links point at files, and the guides match the code._ | docs |

### F. Page layout and CSS don't match

_Found in branch #3 by comparing the class and ID names in `index.html`, the CSS and the HTML that `app.js` generates._

| # | Sev | Problem | Evidence |
|---|---|---|---|
| F1 | ✅ | **`app.js` and the CSS were written together for a page layout that isn't in this project.** Most of `index.html`'s own markup has no styling. This explains the B bugs (missing IDs), the unstyled 3D Viewer and Board Explorer (browser-default buttons, a tiny 3D box), and the "Rendering 3D Model..." text that never goes away. **Decided in [ADR 0002](../decisions/0002-screen-markup.md):** the Viewer and Database switch to the new layout, the Dashboard is added as the home screen, and the other screens keep the current page (F2–F6). | HTML generated by `app.js`: **126 of 143** classes have CSS rules (88%). HTML written in `index.html`: **38 of 217** (18%). CSS ID rules: **21 of 34** target IDs that don't exist in `index.html` (`#viewer-sidebar`, `#viewer-canvas-area`, `#sim-toolbar`, `#ai-input`…). |
| F2 | ✅ | **Shared building blocks are unstyled on every current-page screen:** `glass-panel` (22 uses), `panel`, `panel-header`, `panel-title`, `view-title`, `view-subtitle` and the shared buttons have no CSS. The sidebar and top-bar buttons also showed the browser's default grey (`rgb(239, 239, 239)`), because `.nav-item` and `.icon-btn` never set a background on `<button>` elements. **Fixed in #7:** new `src/styles/components/panels.css` (design tokens only, matching the Dashboard cards). Screen-specific styling stays with F3–F6. | [ADR 0002](../decisions/0002-screen-markup.md) measurements, `tests/smoke/styles.spec.js` |
| F3 | ✅ | **Simulator layout is broken:** it's stacked and unstyled, and the breadboard canvas isn't visible. The CSS styles `#sim-toolbar`, `#sim-palette`, `#sim-canvas-area` and `#sim-instruments` as **IDs**, while the page uses those names as **classes**. Only 7 of the CSS's 26 simulator classes appear in the page. _Measured in #14: `#view-simulator` was a 3×3 grid with one child, so everything sat in its 160 px first column (board 160×112, screen scrolled). The board also took its drawing size from its parent, so drawings were squashed._ _**Fixed in #14:** new `src/styles/views/simulator.css` styles the page's own classes (toolbar, palette, board, instruments, status bar), the two blocking grid rules are removed, and the board draws at its own size (726×558 at 1280×720; tested, also after a resize)._ | [screenshot](../images/f1-simulator-current.png), [simulator.css](../../src/styles/views/simulator.css) |
| F4 | ✅ | **Board Explorer unstyled.** The page and the JS draw the board on a canvas, but the CSS describes an element-based board (`board-visual`, `board-pin-dot`…). The page's classes (`board-tab`, `pin-filter-btn`, `board-info-panel`…) have no CSS. _Measured in #16: everything stacked in one column, the board canvas 174 px tall (the 280 px drawing was cut off), and it grew on every filter click (174 → 270 px after 4), because it was sized from its parent, which also holds the zoom buttons._ _**Fixed in #16:** new `src/styles/views/boards.css` styles the page's own classes and the pin list and specs that `app.js` draws; the board canvas is sized from its own box (tested: 862×529 beside the info panel, no growth, no scrolling)._ | 3 of 23 page classes styled, [boards.css](../../src/styles/views/boards.css), [app.js:1103](../../src/app/app.js) |
| F5 | ✅ | **Datasheet sidebar unstyled** (`ds-list`, `ds-search`, `toc-btn`…). The content area is already styled by the JS. _Measured in #17: 17 of 28 datasheet classes had no CSS rule. The search box was a raw browser input (white background, black Arial, `inset` grey border, 181 px wide in a 218 px box), the 6 section buttons were the browser grey `rgb(239,239,239)` with `cursor: default`, and **the active section button looked exactly like the others**, so you could not see which section you were on. List rows had no padding, no hover and `cursor: auto` despite having an `onclick`, and the name and maker were both 14 px in the same colour._ _**Also found and fixed in #17:** the screen was **cut off at the bottom**. `.datasheet-layout` is 628 px tall with `overflow: hidden`, but both columns were 832 px (1318 px on the Examples section), because `.datasheet-viewer-area` was `display: block` and `#ds-content` grew to its full content height instead of scrolling._ _**Fixed in #17:** new `src/styles/views/datasheet.css` styles the page's own classes with design tokens, and both columns now fit the layout (628 px) with `#ds-content` scrolling inside it (776 px of content in a 507 px box). Tested in `datasheet.spec.js`._ | 4 of 22 page classes styled, [datasheet.css](../../src/styles/views/datasheet.css) |
| F6 | ✅ | **AI message classes don't match the CSS:** the JS generates `ai-msg-avatar` / `ai-msg-content` / `ai-msg-time`, and the CSS styles `ai-message-avatar` / `-content` / `-time`. The suggestion buttons are unstyled. _**Fixed in #11:** the JS uses the CSS names and its structure (`.ai-message-body` holds the bubble and the time). The chips and the page's welcome bubble (`.chat-bubble`) share the existing rules (tested with computed styles)._ | 1 of 5 JS classes styled |
| F7 | ✅ | **AI screen layout was written for a different page.** `#view-ai` had `grid-template-rows: 1fr auto auto` (three rows the page doesn't have) and `padding: 0 !important`. Once messages showed, the chat grew instead of scrolling: after 3 questions the input was at y=1154 on a 720 px screen. The title and chips also touched the sidebar (0 px padding, other screens 20 px). _Found and fixed in #11:_ `grid-template-rows: minmax(0, 1fr)` and no padding override (tested). | [main.css:1912](../../src/styles/) |
| F8 | ✅ | **AI screen markup has inline styles with hard-coded colours** (`#222`, `#07070a`, `#0c0c14`, `#fff`), which override the CSS and ignore the design tokens. The input also shows the browser's white focus outline. _Found in #11._ _Measured in #17b: 12 elements carried 14 inline `style` rules, 790 bytes of the page. The chat box was `#07070a` with a `#222` border, the input `#0c0c14` with `#fff` text, and the input's focus ring was the browser's default (`outline: auto`). The title was also forced to 24 px, while every other screen uses the shared 22 px._ _**Fixed in #17b:** new `src/styles/views/ai.css` holds the layout and colours as design tokens, and `index.html` keeps its markup, classes and IDs with no inline styles left (only the `display` the router sets on the screen itself). Focus is now shown by the border (`--border-active`) with no browser outline, and the title uses the shared 22 px. Tested in `ai.spec.js` (4 tests)._ | [index.html:483-517](../../index.html), [ai.css](../../src/styles/views/ai.css) |

| F9 | ✅ | **`app.js` writes inline styles with hard-coded colours into the HTML it generates.** 33 elements, including the Datasheet's electrical table (`#333`, `#222`) and its code blocks (`background:#07070a; border:1px solid #222`). Same problem as F8, but in the JS instead of the page, so it overrides the stylesheets the same way. The `.ds-table` and `.ds-code` rules in `main.css` already cover most of it. _Found in #17c while adding the Pinout and Package sections, which use the existing classes and add no inline styles._ | [app.js:1451](../../src/app/app.js), [app.js:1461](../../src/app/app.js), [app.js:1508](../../src/app/app.js) _Re-counted in #30: after the earlier fixes, 3 places still hard-coded colours, all in the Datasheet (`views/datasheet.view.js`): the electrical table's header row (`#333`), its rows (`#222`) and the code-example block (`#07070a`, `#222`). The other 30 inline styles use a pin type's colour from data or a design token, which is intended._ _**Fixed in #30:** those blocks have no inline styles; `views/datasheet.css` styles them with the same sizes and spacing and token colours, so they follow the theme. Checked: only the border and code-background colours changed. Tested: no hard-coded colour in the generated inline styles, and the code border changes with the theme._ |

| F10 | ✅ | **8 inline styles with hard-coded colours are left in `index.html`.** The simulated multimeter display (`#050508`, `#00ff88`, `#666`) and two colour swatches (`#555`). The multimeter is meant to look like a real device, so staying dark is right; the swatches are cosmetic. _Found in #19 while removing the 16 `<kbd>` inline styles that broke in light mode._ Same family as F8 and F9. | [index.html:336-359](../../index.html) _**Fixed in #29:** the 5 elements (2 scope dials, multimeter display, value and unit) are styled in `views/simulator.css` with new tokens `--lcd-bg`, `--lcd-border`, `--lcd-text`, `--lcd-dim` and `--dial-ring`, which the light theme deliberately does not override. Computed styles checked identical in both themes; a test checks there are no inline styles and the meter stays dark in light mode._ |
| F11 | ✅ | **Four things stayed dark in the light theme:** the 3D pin details panel (a hard-coded `rgba(8,8,18,0.95)` behind dark text), the search pop-up and its backdrop (colours set as inline styles from JS, which beat the CSS), the shortcuts pop-up and its close button (inline `rgba(0,0,0,0.8)` and `color:#fff`, invisible on a light card), and the Simulator's parts (drawn `#c0c0c0` on a white board, about 1.8:1 contrast). _Found in #31._ _**Fixed in #31:** all four use design tokens — the pop-ups are styled in `components/search-modal.css` and `components/panels.css`, and the Simulator reads `--sim-*` from the theme when it changes. Tested in both themes._ | [viewer.css](../../src/styles/views/viewer.css), [search.js](../../src/ui/search.js), [index.html](../../index.html), [simulator/index.js](../../src/engines/simulator/index.js) |
| F12 | ✅ | **Hard-coded colours were left in the code that writes HTML**, after F9 fixed the Datasheet: the pin type colours used everywhere (badges, pin lists, tooltips, pin details), the Dashboard's stat and protocol cards, and the sample projects' accent colour. A handful were left in the CSS too (`btn-primary`'s gradient and its black text, which was unreadable in the light theme; 3%-white borders that vanished on a light page; toast, badge and scrollbar tints). _Found in #31._ _**Fixed in #31:** `--pin-*`, `--pink`, `--blue`, `--coral`, `--grey`, `--on-accent`, `--sim-*` and `--viewport-bg` are design tokens, project data names a token instead of a hex colour, and a canvas reads the token through the new `utils/css.js`. A test now opens all 9 screens and fails on any hard-coded colour in the HTML they write._ | [tokens.css](../../src/styles/base/tokens.css), [pin-types.js](../../src/app/pin-types.js), [dashboard.view.js](../../src/views/dashboard.view.js) |

### Future ideas (not bugs)

Features of the old 3D Viewer that the new layout doesn't have ([ADR 0002](../decisions/0002-screen-markup.md)). Re-add them only if wanted:
- pin legend (colour key for pin types)
- "example usage" code for the selected pin
- info overlay on the 3D view (component, package, pins, voltage)
- DIP / SMD / QFP / BGA package buttons (never worked)
- screenshot button

Simulator (found in #14b, and #31):
- **upload code to a real board.** The "Upload Code" button was removed in #31, because all it could do was say it wasn't available. A real one needs a browser-to-board link (Web Serial) and a compiler.
- **more parts in the palette.** Since #32 every part the solver simulates has a button (resistor, capacitor, LED, battery, switch, diode, ground). A transistor, op-amp or board would need a model in the solver first (E23 removed the ones it could only draw).
- warn when an LED gets too much current (for example over 30 mA). Today an LED straight across the 9 V battery shows 667 mA and simply lights.
- an "Import" button that opens a file saved with Export (the engine's `loadCircuit()` can already read it).
- probe a part: click a part to show its voltage on the oscilloscope and its voltage, current or resistance on the multimeter (today CH1 is the battery, CH2 the first LED, and the multimeter reads the whole circuit).

AI assistant (found in #12):
- short names for parts: the AI finds a part only by its full name or ID, so "stm32", "blue pill", "555" or "esp32" (for the ESP32-WROOM-32 card) aren't recognised. This needs a list of extra names for each part in `src/data/components.js`.

---

## Part 2 — The fix plan (step by step)

Do the phases **in order**. Each phase ends with a check, so you always know it worked before moving on.

### Phase 0 — Folder structure ✅ DONE

- Project restructured into `src/`, `docs/`, `public/`, `tests/`, `scripts/`, `legacy/` (files moved, code unchanged). See [decisions/0001-folder-structure.md](../decisions/0001-folder-structure.md).

### Phase 1 — Stop the crashes (30 min) → fixes A1–A8

1. ✅ **Load the data file** (branch #3). In [src/main.js](../../src/main.js), add this line **before** `three-viewer.js` and `app.js`:
   ```js
   import './data/data.js';
   ```
2. ~~Guard against "nothing selected"~~. **Not needed:** fixing A1 removes A2. See A2 above.
3. ✅ **Fix the keyboard map** (branch #4). Keys follow the sidebar order, so they update when screens are added. After #8:
   `1 dashboard · 2 viewer · 3 simulator · 4 database · 5 boards · 6 datasheet · 7 ai · 8 projects · 9 settings`.

✅ **Check:** run `npm run dev`, open the browser console (F12), and click every menu item. There should be **zero red errors**, and a 3D chip should appear in the viewer.

### Phase 2 — Pick one version and delete the other (30 min) → fixes E1 ✅ DONE (#6)

Keep the **new** code (`app.js` + `data.js`). It is bigger and has the 3D models, AI answers, datasheets and projects.

1. ~~Copy anything useful from `legacy/database.js` into `src/data/data.js` first.~~ **Changed (ADR 0002 analysis):** the old data uses a different format, and adding the resistor and capacitor now would show them as an 8-pin chip until D8 is fixed. So they're listed under E1 instead, and restored from git in the branch that needs them.
2. ✅ Delete the `legacy/` folder, and its entry in `eslint.config.js`. (The `database.js` import was already removed from `src/main.js` during the restructure.)

✅ **Check:** the app still runs with no errors, and `grep -r "ComponentDatabase\|appState" src/` returns nothing.

### Phase 3 — Reconnect the page to the code (2–4 hours) → fixes B1–B9, C1–C6

**Rule ([ADR 0002](../decisions/0002-screen-markup.md)):** follow the per-screen table. The Viewer, Database (→ Component Library) and Dashboard use the markup the JS and CSS expect. All other screens keep `index.html`, and the JS and CSS adapt to it.

> **Replaced by ADR 0002:** steps 2–3 (Viewer: use the new layout instead of rewiring the old panel), step 5 (Database: use the Component Library instead of writing `renderDatabase()`), and step 9 (Dashboard: added as the home screen). Steps 1, 4, 6, 7 and 8 still apply. The styling work is F2–F6.

1. **Rename IDs in the JS** using table B:
   - ✅ `ai-chat-area → ai-chat-messages`, `ai-input → ai-user-query`, `ai-send → ai-send-btn` (#11)
   - ✅ `mm-value → multimeter-val`, `mm-unit → multimeter-unit`, `mm-mode-select → mm-mode` (#14)
   - `pin-detail-panel → pin-info-panel`
2. **Rewrite `renderViewerSidebar` / `renderPinTable`** so they fill the elements that exist:
   - Part list → `#component-list` (one row per `CircuitLabData.components`, click → `selectComponent(id)`)
   - Filter → `#component-filter` + the `.cat-btn` buttons → `setFilter(cat)`
   - Pin details → `#pin-number-badge`, `#pin-name-display`, `#pin-type-badge`, `#pin-voltage`, `#pin-current`, `#pin-protocol`, `#pin-direction`, `#pin-alt`, `#pin-example-code`
   - HUD → `#hud-component`, `#hud-package`, `#hud-pins`
3. **Hook up the 3D toolbar** (in `initViewerPanel`, once):
   `btn-rotate → ThreeViewer.setAutoRotate`, `btn-wireframe → setViewMode('wireframe')`, `btn-explode → setViewMode('explode')`, `btn-reset-cam → ThreeViewer.resetView`, `btn-pins` → show/hide `#pin-labels-layer`, `btn-screenshot` → `renderer.domElement.toDataURL()` + download.
4. ✅ **Hook up the simulator toolbar** (once, see Phase 4.3; done in #14):
   `sim-run → CircuitSimulator.startSim`, `sim-pause/sim-stop → stopSim`, `sim-clear → resetSim`, `sim-export → exportCircuit`, `sim-speed → setSimSpeed(value)`, `ws-add-resistor/led/capacitor/ic/wire → addComponentToCanvas(type)`.
5. **Database screen:** write `renderDatabase()` that fills `#db-components-grid` using the existing `renderComponentCard()`. Connect `#compare-mode-btn` / `#close-matrix-btn` to the existing `toggleCompare` / `clearCompare`, and fill `#comparison-table-body`.
6. ✅ **Projects (C4, #18):** point `renderProjects` at `#projects-grid` and `#create-project-btn → newProject()`.
7. ✅ **Settings (C5, #19; a `data-theme` attribute on `<html>` instead of a class):** `theme-btn-toggle` and `theme-toggle` toggle a `light` class on `<body>`. Save the choice in `localStorage`.
8. ✅ **Top bar (C6, #20):** the notification bell opens and closes `#notif-drawer`, `#clear-notifs` empties it, and `?` opens `#shortcuts-modal`.
9. ✅ **Delete the dead code paths** (#8 added the Dashboard, #10 removed `components`) for `dashboard` and `components` in `navigateTo`, or add those screens to the HTML (decide one).

✅ **Check:** click every button on every screen. Each one should visibly do something, and there should be no console errors.

### Phase 4 — Fix the logic bugs (1–2 hours) → fixes D1–D9

1. ✅ **AI matcher (D1, #12):** replace the first/second-word check with keyword lists per answer, and pick the answer with the most keyword hits:
   ```js
   const topics = [
     { keys: ['resistor', 'ohm', 'led'], answer: 'What resistor do I need for an LED at 5V?' },
     { keys: ['i2c', 'sda', 'scl'],      answer: 'How do I wire an I2C sensor to Arduino?' },
     // ...
   ];
   ```
   Check specific part names **before** generic topics.
2. ✅ **Safe chat HTML (D6, D7, #13):** run `escapeHtml(text)` first, then `formatMarkdown`. Move the ```` ``` ```` block rule **above** the single-backtick rule.
3. ✅ **Initialize once (D3, #15):** add an `initialized` flag in `CircuitSimulator.init` (and the toolbar wiring) so it only runs the first time.
4. ✅ **One background (D4, #21):** keep either `circuit-bg.js` or `initBackground()` in `app.js`, not both.
5. ✅ **Pause hidden loops (D5, #9 and #15):** in `navigateTo`, pause the 3D/simulator loops when leaving their screen, and resume when you come back.
6. ✅ **3D model cases (D8, #23):** make the `switch` in `loadComponent` match the real IDs in `data.js`.
7. ✅ **Hash routing (D9, #22):** add a `hashchange` listener that calls `navigateTo(location.hash.slice(1))`, and set the hash inside `navigateTo`. Page refresh and the back button then work too.

✅ **Check:** ask the AI "how does an esp32 work" and get the ESP32 answer. Visit the simulator 5 times, then click once and get exactly 1 part.

### Phase 5 — Cleanup & dependencies (1 hour) → fixes E2–E7, E9

1. ✅ (E3, #25) Remove the `<link rel="stylesheet" href="/src/styles/main.css">` from `index.html` (Vite already loads it through `main.js`).
2. ✅ (E2, #25) Either add a real `manifest.json` + `favicon` or remove the `<link rel="manifest">` line.
3. ✅ (E4, #25) Delete the empty `assets/` folders, or put the real fonts and icons there.
4. ✅ (E5, #25) Replace the fake notifications with real ones pushed from `showToast()`.
5. ✅ (E6, #26) Run `npm audit fix` (safe, fixes `nanoid` + `postcss`). Plan a separate upgrade to Vite 6+ later, since it's a breaking change. _Done together: Vite 8.3.0._
6. ✅ (E7, #24) Optional: install Three.js with `npm i three` and `import * as THREE from 'three'` instead of the r128 CDN script, so the app works offline.
7. ✅ (E9, #30) Run `npm run build` to regenerate `dist/`. _`dist/` is ignored by git, so every build is fresh._

✅ **Check:** `npm audit` shows 0 high issues, `npm run build` succeeds, and `npm run preview` works.

### Phase 6 — Make it professional (half a day) → fixes E8

1. ✅ **README.md**: what the app is, how to run it, the folder map, screenshots.
2. ✅ **Linter**: ESLint 10 with the recommended rules (`npm run lint`). New mistakes are errors; the 27 known leftovers are capped warnings (E11). _Correction:_ ESLint **can't** catch A1-type bugs while files share names through `window`, because it can't tell a loaded file from a forgotten one. The smoke tests catch them at runtime, and moving to real `import`/`export` (the `refactor/split-*` branches) will make them build errors. Prettier is postponed until after the refactor, so it doesn't cause merge conflicts in every fix branch.
3. ✅ **Smoke test**: Playwright tests that open every screen, search, send an AI message and press shortcuts, and **fail if any error appears**. Run with `npm run test:smoke`. Known bugs are tracked as expected failures.
4. ✅ **Version control:** one branch per issue, merged through Pull Requests. See [GIT_WORKFLOW.md](../guides/GIT_WORKFLOW.md) for the full branch plan.

### Phase 7 — Optional upgrades

- **Real AI answers:** call the Claude API from a small backend (for example a Node/Express or serverless function). **Never put an API key in browser JavaScript.**
- ✅ **Save projects properly:** done in #18 (C4): your projects are saved under `circuitlab.my-projects`. The old read-only `loadProjects()` was removed in #25b (E16).
- ✅ **Move to real ES modules:** done in #27a–c ([ADR 0003](../decisions/0003-es-modules.md)). Files share code with `import` / `export`; only `CircuitApp`, `CircuitLabData`, `ThreeViewer` and `CircuitSimulator` stay on `window`. This makes mistakes like A1 impossible.

### Phase 8 — Last pass (#31) → fixes D31–D48, E18, E20–E22, F11, F12

1. ✅ The whole of `index.html`, `src/` and the docs read again, top to bottom, after everything else was done.
2. ✅ Every new bug got a test that failed before the fix and passes after it (36 new tests, all 36 failing first).
3. ✅ Dead code removed: ~90 unused CSS rules, the "Upload Code" and "NE555" buttons that could only say "not available", a hidden project card, `state.searchQuery`, unused part fields, empty folders and five unused entries in the `CircuitApp` API.
4. ✅ Docs: every link checked by a test, this plan and the branch plan finished, and the README, guides and CLAUDE.md brought up to date.

✅ **Check:** `npm test` (213 tests), `npm run build` with no warning, and `npx playwright test tests/smoke --repeat-each=4` with 0 unexpected.


### Phase 9 — Audit pass (#32) → fixes D49–D54, E23–E27

1. ✅ All code and docs read again; every finding checked by running it, not guessed.
2. ✅ Each bug got a test that fails on the code before the fix and passes after it (7 new tests, all 7 failing first).
3. ✅ Dead code, unused exports, old comments and misleading text removed; stale docs brought up to date.

✅ **Check:** `npm test` (220 tests), `npm run build` with no warning. _Done on `main` directly, without a branch or PR, at the owner's request for this one pass._
---

## Time estimate

| Phase | Effort | Result |
|---|---|---|
| 0 | 10 min | Can undo anything |
| 1 | 30 min | **No more crashes** |
| 2 | 30 min | One clean codebase |
| 3 | 2–4 h | **Every button works** |
| 4 | 1–2 h | Correct behaviour |
| 5 | 1 h | Clean + secure deps |
| 6 | ½ day | Professional project |
| 8 | 1 day | **Everything in this report fixed** |

See [LEARNING_GUIDE.md](../guides/LEARNING_GUIDE.md) for a plain-English explanation of how the code works.
