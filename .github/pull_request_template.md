## What

<!-- One or two lines: what this PR changes. -->

## Why

<!-- The roadmap row (e.g. R1a, docs/tracking/PROGRESS.md) or bug code (e.g. D55, docs/tracking/BUGS.md). -->
Row / fixes:

## How it was tested

- [ ] `npm test` passes (lint with 0 warnings, and the smoke tests)
- [ ] `npm run build` succeeds
- [ ] A test fails before this change and passes after it (both results below), or a refactor's before/after snapshot matches
- [ ] No red errors in the browser console on the affected screens
- [ ] `knownBug()` markers removed for the issues this PR fixes

<!-- Paste the before/after test results here. -->

## How to check it yourself

<!-- Steps the owner can follow in the browser (preview link from R0b on). -->

## Checklist

- [ ] Base branch: the current `phase/*` branch for a feature PR; `main` only for a phase PR or a hotfix
- [ ] The branch does one thing only
- [ ] Docs updated in this PR: `docs/tracking/PROGRESS.md`, `docs/context/PROJECT_CONTEXT.md`, `docs/context/SESSION_LOG.md`, `CHANGELOG.md`, and any other doc it touches
- [ ] New bugs noticed but not fixed are in `docs/tracking/BUGS.md`
- [ ] No secrets, `dist/` or `node_modules/` committed

Full checklists: `docs/rules/REVIEW_CHECKLIST.md`.
