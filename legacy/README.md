# legacy/

Old code from an earlier version of the app. **Nothing here is loaded by the app.**

| File | What it was | Plan |
|---|---|---|
| `script.js` | The old app controller. The current `index.html` was designed for it. | Use as a reference for element IDs while fixing, then delete. |
| `database.js` | The old component data (`window.ComponentDatabase`). | Merge anything useful into `src/data/`, then delete. |

This folder will be removed in Phase 2 of [docs/FIX_PLAN.md](../docs/FIX_PLAN.md), after anything useful has been copied out.
